from setuptools import setup, find_packages

setup(name='tle_parser',
      version='0.1',
      description='Parse TLE and predict orbits',
      author='FranMark',
      packages=find_packages(),
      zip_safe=False)